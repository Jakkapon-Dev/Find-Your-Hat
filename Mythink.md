# 🧠 My Think & Dev Log (บันทึกแนวคิดและขั้นตอนการพัฒนาเกม Find Your Hat)

เอกสารนี้เขียนขึ้นเพื่อบันทึกกระบวนการคิด วิธีการออกแบบโค้ด และขั้นตอนการลงมือทำเกม **Find Your Hat** ตั้งแต่เริ่มศูนย์จนเสร็จสมบูรณ์

---

## 🎯 1. จุดเริ่มต้นและแนวคิดของเกม (Concept & Goal)

โจทย์ของโปรเจกต์นี้คือการสร้างเกม Terminal 2D ง่ายๆ ที่ผู้เล่นต้องเดินตามหาหมวก (`^`) และต้องระวังไม่ให้เดินตกหลุม (`O`) หรือหลุดออกนอกขอบกระดาน

### สิ่งที่ตั้งใจไว้ก่อนเริ่มเขียนโค้ด:
1. **ใช้โครงสร้าง Class (OOP)** เพื่อให้จัดการ State ของเกม (พิกัด x, y, จำนวนก้าว, ตารางสนาม) ได้เป็นระบบ
2. **ไม่ใช้ Library เสริมที่ไม่จำเป็น** เน้นใช้ Vanilla JavaScript และโมดูล Built-in อย่าง `readline`
3. **UI ต้องอ่านง่ายและดูดี** มีสีสันเพื่อแยกองค์ประกอบต่างๆ ให้ผู้เล่นไม่งง

---

## 🪜 2. ขั้นตอนการพัฒนาทีละ Step (Step-by-Step)

### Step 1: ออกแบบโครงสร้างแผนที่ (Data Structure)
- แผนที่เกม 2 มิติ เหมาะที่สุดกับการใช้ **2D Array (`Array of Arrays`)**
- แกน Y คือแถว (Row) และแกน X คือคอลัมน์ (Column)
- กำหนดสัญลักษณ์ง่ายๆ:
  - `*` แทนตำแหน่งผู้เล่นและรอยเท้า
  - `^` แทนหมวกเป้าหมาย
  - `O` แทนหลุม
  - `·` แทนพื้นที่สนามว่าง

---

### Step 2: จัดการระบบรับ Input และการเคลื่อนที่
- ใช้ `readline.createInterface` ของ Node.js เพื่อรอรับค่าจากคีย์บอร์ด
- กำหนดปุ่มมาตรฐานเกม: `W` (ขึ้น), `S` (ลง), `A` (ซ้าย), `D` (ขวา)
- **จุดที่ต้องระวังตอนคิด logic พิกัด:**
  - เดินขึ้น (`W`) -> ลดค่า `y` ลง 1 (`this.y -= 1`)
  - เดินลง (`S`) -> เพิ่มค่า `y` ขึ้น 1 (`this.y += 1`)
  - เดินซ้าย (`A`) -> ลดค่า `x` ลง 1 (`this.x -= 1`)
  - เดินขวา (`D`) -> เพิ่มค่า `x` ขึ้น 1 (`this.x += 1`)

---

### Step 3: เขียน Logic การตรวจจับเงื่อนไข (Game Conditions)
ต้องตรวจตามลำดับความสำคัญก่อน-หลัง:
1. **เช็คตกขอบก่อนเสมอ (`isInBounds`)**: ป้องกัน Error `Cannot read property of undefined` เมื่อ array index หลุดขอบ
2. **เช็คตกหลุม (`isHole`)**: ตรวจสอบว่าช่องที่เหยียบมีค่าเป็น `'O'` หรือไม่
3. **เช็คเจอหมวก (`isHat`)**: ตรวจสอบว่าช่องที่เหยียบเป็น `'^'` ถ้าใช่คือชนะ!
4. **ถ้าไม่โดนอะไรเลย**: บันทึกตำแหน่งปัจจุบันด้วย `'*'` แล้วเรียก `playTurn()` เพื่อรอรับคำสั่งตาถัดไป

---

### Step 4: สุ่มสร้างแผนที่อัตโนมัติ (`generateField`)
อยากให้เกมไม่น่าเบื่อและเล่นได้เรื่อยๆ เลยต้องเขียนฟังก์ชันสุ่มแผนที่:
- สร้าง Array เปล่าตามขนาด `height` x `width`
- **สุ่มวางหมวกก่อน**: ใช้ `Math.random()` แต่ใช้ `do...while` ดักไม่ให้หมวกสุ่มตกที่พิกัด `(0, 0)` ซึ่งเป็นจุดเริ่ม
- **สุ่มกระจายหลุม**: วนลูปทุกช่องแล้วเทียบกับเปอร์เซ็นต์ (Default 20%) ถ้าสุ่มได้เลขน้อยกว่าเปอร์เซ็นต์จะเปลี่ยนช่องนั้นเป็นหลุม (เว้นจุดเริ่มและจุดหมวกไว้)

---

### Step 5: ตกแต่ง UI ด้วยสีสัน (ANSI Colors) 🎨
ตอนแรกที่รัน เกมเป็นสีขาว-ดำธรรมดา ดูแล้วตาลาย แยกหลุมกับทางเดินยาก
> **Note:** ในส่วนนี้ **ผมได้ใช้ AI ช่วยแนะนำรหัส ANSI Escape Codes** สำหรับทำสีข้อความใน Terminal เพื่อให้เกมมีมิติและสวยงามยิ่งขึ้น:
>
> ```javascript
> const colors = {
>   reset: '\x1b[0m',
>   cyan: '\x1b[36m',     // สีฟ้าตัวละคร/ทางเดิน
>   yellow: '\x1b[33m',   // สีเหลืองทองหมวก
>   red: '\x1b[31m',      // สีแดงเตือนหลุม/แพ้
>   gray: '\x1b[90m',     // สีเทากรอบและพื้นหลัง
>   bold: '\x1b[1m',      // ตัวหนา
>   green: '\x1b[32m'     // สีเขียวตอนชนะ
> };
> ```
> การเพิ่มสีทำให้เกมดูเป็นมิตรกับผู้เล่นมากขึ้นอย่างเห็นได้ชัด

---

### Step 6: เพิ่มระบบ Replay และนับก้าว (Quality of Life)
- เพิ่มตัวแปร `this.steps` เพื่อดูว่าผู้เล่นใช้ไปกี่ก้าวกว่าจะเจอหมวก
- เพิ่มฟังก์ชัน `askPlayAgain()` เพื่อให้ผู้เล่นสามารถกด `y` เพื่อสร้างแมพใหม่เล่นต่อได้ทันทีโดยไม่ต้องสั่งรันคำสั่ง node ใหม่

---

## 🔍 3. อธิบายโค้ดแต่ละส่วนอย่างละเอียด

### 1. การสร้าง Class Field & Constructor
```javascript
class Field {
  constructor(field = [[]]) {
    this.field = field;
    this.x = 0;
    this.y = 0;
    this.steps = 0;
    this.field[0][0] = pathChar; // จุดสตาร์ทมุมซ้ายบน
  }
}
```
**วิธีคิด:** ใช้ constructor กำหนดค่าเริ่มต้นของเกม โดยผู้เล่นจะเกิดที่มุมบนซ้าย `(0, 0)` เสมอ และมาร์กจุดแรกเป็น `*` ทันที

---

### 2. ฟังก์ชัน `print()` (เรนเดอร์หน้าจอ)
```javascript
print() {
  console.clear();
  // ... แสดงหัวข้อและจำนวนก้าว ...
  for (let row of this.field) {
    let line = `${colors.gray}|${colors.reset}`;
    for (let cell of row) {
      if (cell === pathChar) line += `${colors.cyan}${colors.bold} * ${colors.reset}`;
      else if (cell === hat) line += `${colors.yellow}${colors.bold} ^ ${colors.reset}`;
      else if (cell === hole) line += `${colors.red} O ${colors.reset}`;
      else line += `${colors.gray} · ${colors.reset}`;
    }
    line += `${colors.gray}|${colors.reset}`;
    console.log(line);
  }
}
```
**วิธีคิด:** สั่ง `console.clear()` ก่อนทุกครั้งเพื่อให้ Terminal วาดทับตำแหน่งเดิม (ไม่เลื่อนลงยาวเป็นหางว่าว) จากนั้นวนลูปเติมสีทีละช่องพร้อมตีกรอบซ้าย-ขวา

---

### 3. ฟังก์ชัน `playTurn()` (Game Loop)
```javascript
playTurn() {
  this.print();
  rl.question('Enter move: ', (input) => {
    const move = input.trim().toLowerCase();
    // ... คำนวณพิกัดใหม่ ...
    // ... เช็คเงื่อนไขแพ้ / ชนะ ...
    this.field[this.y][this.x] = pathChar;
    this.playTurn(); // วนลูปกลับมาเล่นตาถัดไป
  });
}
```
**วิธีคิด:** ใช้เทคนิค **Recursion** เรียก `this.playTurn()` ซ้ำเรื่อยๆ ตราบใดที่ยังไม่เข้าเงื่อนไขจบเกม ทำให้เกมรันไปได้เรื่อยๆ โดยไม่ต้องใช้ While Loop ซับซ้อน

---

## 💡 4. ปัญหาที่เจอระหว่างทำ & วิธีแก้ (Challenges & Fixes)

1. **ปัญหาพิกัดสับสนระหว่าง X กับ Y ใน 2D Array**:
   - *ปัญหา:* ในระบบพิกัดคณิตศาสตร์ เรามักคุ้นเคยกับ `(X, Y)` แต่ใน Array 2D แถวคือแนวตั้ง (Y) และหลักคือแนวนอน (X) เวลาเข้าถึงข้อมูลต้องเขียนเป็น `this.field[this.y][this.x]`
   - *วิธีแก้:* ตั้งชื่อตัวแปรให้ชัดเจนเป็น `this.x` และ `this.y` และตรวจสอบลำดับ index ให้แม่นยำ

2. **ปัญหาหมวกสุ่มทับจุดเริ่มต้น**:
   - *ปัญหา:* บางรอบที่รัน เกมจบตั้งแต่ตาแรกเพราะหมวกสุ่มไปอยู่ที่ `(0, 0)`
   - *วิธีแก้:* ใช้เงื่อนไข `while (hatX === 0 && hatY === 0)` ใน loop สุ่มหมวก บังคับให้สุ่มใหม่ถ้าตรงกับพิกัดเริ่มต้น

3. **ปัญหาการปิดโปรแกรมใน Node.js**:
   - *ปัญหา:* เมื่อจบเกม ถ้าไม่สั่ง `rl.close()` โปรแกรมจะค้างรอ input อยู่ใน Terminal
   - *วิธีแก้:* สั่ง `rl.close()` เมื่อผู้เล่นเลือกออก หรือตอบ `n` ในหน้าจอ Play again

---

## 📝 5. สรุปภาพรวม
โปรเจกต์นี้ช่วยให้เข้าใจเรื่อง **Class OOP**, **2D Array Manipulation**, **Asynchronous I/O (`readline`)**, และ **Recursion** ใน JavaScript ได้ชัดเจนขึ้นมาก และการนำ ANSI Codes มาผสมผสานทำให้เกมดูน่าเล่นและสมบูรณ์ยิ่งขึ้นครับ!
